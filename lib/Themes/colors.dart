import 'package:flutter/material.dart';

const darkGrey = Color(0xFF222222);
const lightGrey = Color(0xFF7C7C7C);
const white = Colors.white;

const green = Color(0xFF36C736);
const red = Color(0xFFD0021B);

const greenGradient = LinearGradient(
  colors: [Color(0xFF11998e), Color(0xFF38ef7d)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const meridianGradient = LinearGradient(
  colors: [Color(0xFF283c86), Color(0xFF45a247)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const sunkistGradient = LinearGradient(
  colors: [Color(0xFFF2994A), Color(0xFFF2C94C)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const orkaGradient = LinearGradient(
  colors: [Color(0xFF44A08D), Color(0xFF093637)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const purpleGradient = LinearGradient(
  colors: [Color(0xFF41295a), Color(0xFF2F0743)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);
